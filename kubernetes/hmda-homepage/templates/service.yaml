apiVersion: v1
kind: Service
metadata:
  name: {{ template "hmda-homepage.fullname" . }}
  labels:
    app: {{ template "hmda-homepage.name" . }}
    chart: {{ template "hmda-homepage.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "hmda-homepage.name" . }}
    release: {{ .Release.Name }}
